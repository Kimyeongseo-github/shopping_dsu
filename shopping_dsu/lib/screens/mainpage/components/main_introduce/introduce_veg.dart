import 'package:flutter/material.dart';

class Introduceveg extends StatelessWidget {
  const Introduceveg({super.key});
  static String routeName = "/introduceveg";

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

// 채소 분류 소개 상세 페이지
